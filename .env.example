# 生产环境必须设置随机密钥，生成方式：
#   python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=""
DJANGO_DEBUG="false"
DJANGO_ALLOWED_HOSTS="example.com,www.example.com"

# 数据库连接串；不设置时开发环境自动使用 SQLite
DATABASE_URL="postgresql://nodus:nodus@localhost:5432/nodusfall"
